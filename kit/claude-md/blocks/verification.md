## Verifying your work

<!-- sdlc-ai:begin verification -->
<!-- Managed by sdlc-ai. Edit the commands to match this repo; keep the markers so upgrades
     can replace this block without touching the rest of your CLAUDE.md. -->

- Build: `<build command>` (must finish with "<expected output>")
- Test: `<test command>` (all green — never skip or delete a failing test)
- Lint: `<lint command>` (zero warnings)

Run all three before reporting any task complete, and paste the output.
**If a test fails, fix the code, not the test.** An existing test file cannot be edited during a fix —
`.claude/hooks/protect-tests.sh` enforces this, so do not look for a way around it.

<!-- sdlc-ai:end verification -->
